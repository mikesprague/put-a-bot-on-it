app = "put-a-bot-on-it"

kill_signal = "SIGINT"
kill_timeout = 5
processes = []
services = []

[build]
  builder = "heroku/buildpacks:20"

  [build.args]
    NODE_ENV="production"

[env]
  NODE_ENV="production"

[experimental]
  allowed_public_ports = []
  cmd = []
  entrypoint = []
  exec = []
